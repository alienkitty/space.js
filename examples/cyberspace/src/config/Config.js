export const isMobile = !!navigator.maxTouchPoints;
export const isDebug = /[?&]debug/.test(location.search);

export const basePath = '/examples/cyberspace/public';
export const assetPath = '/examples/assets';
